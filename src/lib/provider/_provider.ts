/**
 * This module re-exports all the exports from the `provider` and `provider.component` modules,
 * and a specific export `register` from `_register` module.
 *
 * @module _provider
 */
export * from './provider'
export * from './provider.component'
export { register } from './_register'