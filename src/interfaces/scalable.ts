/**
 * The Scalable interface represents a UI element that can be scaled.
 *
 * @interface Scalable
 */
export interface Scalable {
  /**
   * The scale factor of the UI element
   */
  readonly scaleFactor: number;
}